package ru.gothmog.isogd.core.gis;

import org.apache.log4j.Logger;

/**
 * Утилита перехода к ГИС-системам. Способ отправки объектов из ИСОГД -
 * обращение по url (обычно на localhost) TODO обратный переход. Объекты делятся
 * на типы, передается тип объекта и его свойства.
 *
 * @author d.grushetskiy
 *
 */
public class GisLinker {

    private static Logger logger = Logger.getLogger(GisLinker.class);
}
